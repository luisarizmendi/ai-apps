# Script for downloading models from Huggingface

## Description

Download models from Huggingface into the `models` directory

## Usage

```bash
./run <model>
```

Example:

```bash
./run Ultralytics/YOLOv8
```

> **Note:**
> Defaults to `facebook/detr-resnet-101`